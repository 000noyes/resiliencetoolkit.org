---
import BaseLayout from '@/layouts/BaseLayout.astro';
import ChangelogEntry from '@/components/ChangelogEntry.astro';
import NewsletterSubscribe from '@/components/NewsletterSubscribe.astro';
import { getSortedEntries } from '@/data/changelog';

const entries = getSortedEntries();
---

<!-- STATUS 2026-03-28: In development. Not linked from site navigation.
     Content may be incomplete or out of date.
     Link in Footer.astro is commented out — restore it when this page is current. -->

<BaseLayout
  title="Changelog | Resilience Hub Toolkit"
  description="What's new in the Resilience Hub Toolkit."
>
  <article class="container mx-auto px-4 py-8 max-w-3xl">
    <!-- Page Header -->
    <header class="mb-8">
      <h1 class="text-hero font-semibold mb-4 text-foreground">Changelog</h1>
      <p class="text-body-large text-muted-foreground">
        New in the toolkit.
      </p>
    </header>

    <!-- Changelog Entries -->
    <div class="changelog-entries">
      {entries.map(entry => (
        <ChangelogEntry
          id={entry.id}
          date={entry.date}
          title={entry.title}
          summary={entry.summary}
          sections={entry.sections}
          tags={entry.tags}
        />
      ))}
    </div>

    <!-- Empty State -->
    {entries.length === 0 && (
      <div class="text-center py-16">
        <p class="text-body-large text-muted-foreground">
          No entries yet.
        </p>
      </div>
    )}

    <!-- Bottom Subscribe CTA -->
    <div class="mt-12">
      <NewsletterSubscribe
        title="Get updates by email"
        description="Get an email when new resources are added."
      />
    </div>

    <!-- Back Navigation -->
    <nav class="mt-8 pt-8 border-t border-border">
      <a
        href="/"
        class="inline-flex items-center text-muted-foreground hover:text-foreground transition-colors"
      >
        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
        </svg>
        <span>Back to Home</span>
      </a>
    </nav>
  </article>
</BaseLayout>
