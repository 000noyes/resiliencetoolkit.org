// MDX Components
// These components are made available to all MDX files

import Todo from '@/design-system/blocks/Todo';
import EditableTable from '@/design-system/blocks/EditableTable';
import InteractiveChecklist from '@/design-system/blocks/InteractiveChecklist';
import ChecklistSection from '@/design-system/blocks/ChecklistSection';
import ChecklistRow from '@/design-system/blocks/ChecklistRow';

export const components = {
  Todo,
  EditableTable,
  InteractiveChecklist,
  ChecklistSection,
  ChecklistRow,
};

export default components;
